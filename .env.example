# Copy to .env and change before sharing the site
ADMIN_PASSWORD=cainton-admin

# For VPS deployment with HTTPS (docker compose --profile https):
# the domain guests will visit — its A record must point at this server
DOMAIN=rsvp.yourdomain.com

# For Cloudflare named-tunnel deployment (docker compose --profile domain):
# TUNNEL_TOKEN=
